package problem1secondtry;

//window class from page 216, figure 9.24
public class Window {
    public Node pred, curr;

    public Window(Node pred, Node curr) {
        this.pred = pred;
        this.curr = curr;
    }
}
